Task 14: Second MaximumProblem Description:The sequence consists of natural numbers and ends with the number 0. Determine the value of the second largest element in this sequence (that is, the element that will be the largest if exactly one maximum value is removed from the sequence). It is guaranteed that there are at least two elements in the sequence.Note: Using arrays is strictly forbidden.
Task 16: Sum of the Sequence - 2Problem Description:Write a program that calculates the sum of all entered numbers. The sequence terminates only when the user enters two zeros (0) consecutively (one right after the other).Note: Using arrays is strictly forbidden.
Task 18: Fibonacci IndexProblem Description:A natural number A is given. Determine its position (index) in the Fibonacci sequence. The sequence is indexed as follows: F₀ = 0, F₁ = 1, F₂ = 1, F₃ = 2, F₄ = 3, F₅ = 5, etc. If the given number A is not a Fibonacci number at all, print -1.Note: Using arrays is strictly forbidden.
Task 20: The Divider-by-Two SystemProblem Description:The user inputs two natural numbers A and B (where A > B). The machine has only two valid operations:-1 (Subtract 1 from the current number).:2 (Divide the current number by 2, allowed only if the number is even).Find the shortest path (the minimum number of commands) to transform number A into number B, and print the executed commands line by line.
Task 21: Maximum Sequence of Equal ElementsProblem Description:A sequence of natural numbers is entered, ending with a single 0. Determine the maximum number of consecutive equal elements (the length of the longest continuous chain of identical numbers) in this sequence.Note: Using arrays is strictly forbidden.T
ask 25: RemaindersProblem Description:Four natural numbers a, b, c, and d are given (where 0 ≤ c < d). Print all numbers in the inclusive range from a to b that yield a remainder of c when divided by d. The output must be in ascending order.Note: Using the if statement inside the loop body is strictly forbidden.
Task 26: Remarkable Numbers - 1Problem Description:Find and print all two-digit numbers (integers from 10 to 99) that are exactly equal to twice the product of their individual digits.
Task 27: Remarkable Numbers - 2Problem Description:Find and print all three-digit numbers (integers from 100 to 999) whose square ends with the digits of the number itself (i.e., \((X \times X) \pmod{1000} == X\)).
Task 29: Remarkable Numbers - 4 (Palindromes)Problem Description:The user inputs two four-digit integers A and B (where A ≤ B). Find and print all numbers in the inclusive range between A and B that are palindromes (numbers that read the same backwards as forwards, such as 1221 or 4554).
